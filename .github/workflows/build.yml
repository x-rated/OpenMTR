# ==========================================================================
#  GitHub Actions — build OpenMTR for Windows, macOS and Linux
#
#  Builds static Windows AMD64 + ARM64 binaries, a macOS ARM64 app bundle,
#  and a Linux x86_64 AppImage in parallel, and uploads them as artifacts
#  (each downloads as OpenMTR-Windows-<ARCH>.zip / OpenMTR-macOS.zip /
#  OpenMTR-Linux-x86_64.zip, containing a plain OpenMTR.exe / OpenMTR.dmg /
#  OpenMTR-x86_64.AppImage respectively). Triggered on pushes to main, pull
#  requests, or manually.
# ==========================================================================

name: Build

# When the workflow runs.
on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths-ignore:
      - '**.md'
      - '.gitignore'
      - 'LICENSE'
  pull_request:
    branches: [ main ]

# Cancel an in-progress run when a newer commit is pushed.
concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

# Least-privilege token: read-only access to the repo.
permissions:
  contents: read

# Shared configuration.
env:
  QT_VERSION: 6.11.1
  # No cache-busting counter here on purpose: the Qt caches key off a hash of
  # the configure flags in ci/qt-*.conf, so they invalidate exactly when that
  # configuration changes and never need a manual bump (forgetting one meant
  # silently shipping artifacts built by the previous configuration).
  #
  # Pin ONE MSVC toolset across both runner images so the AMD64 and ARM64
  # builds link with identical mitigations and codegen (they otherwise resolve
  # different toolsets, e.g. 14.51 vs 14.44). If a job errors that this toolset
  # is unavailable on its runner image, set this to a version present on both
  # windows-latest and windows-11-arm.
  MSVC_TOOLSET: '14.44'

# Build matrix: one job per target architecture.
jobs:
  build-windows:
    strategy:
      matrix:
        include:
          - arch: amd64
            label: Windows AMD64
            runner: windows-latest
            prefix: C:\Qt-static
            artifact: OpenMTR-Windows-AMD64
          - arch: arm64
            label: Windows ARM64
            runner: windows-11-arm
            prefix: C:\Qt-static
            artifact: OpenMTR-Windows-ARM64

    runs-on: ${{ matrix.runner }}
    name: Build (${{ matrix.label }})

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1

      - name: Restore Qt cache
        id: qt-cache
        uses: actions/cache@v6
        with:
          path: ${{ matrix.prefix }}
          key: qt-static-${{ env.QT_VERSION }}-${{ matrix.arch }}-${{ hashFiles('ci/qt-windows.conf') }}

      - name: Setup MSVC
        # ilammy/msvc-dev-cmd hasn't been touched since Jan 2024 and still
        # ships on Node 20. This is TheMrMilchmann's actively maintained
        # fork — same `arch`/`toolset` inputs (so MSVC_TOOLSET pinning is
        # unaffected), just on the node24 runtime.
        uses: TheMrMilchmann/setup-msvc-dev@v4
        with:
          arch: ${{ matrix.arch == 'arm64' && 'amd64_arm64' || 'amd64' }}
          toolset: ${{ env.MSVC_TOOLSET }}

      - name: Ensure Ninja
        shell: pwsh
        run: if (-not (Get-Command ninja -ErrorAction SilentlyContinue)) { choco install ninja -y }

      - name: Fetch & patch Qt source
        if: steps.qt-cache.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          # Series (6.11) derived from QT_VERSION (6.11.1), not written out
          # again: hardcoding it means bumping QT_VERSION alone silently
          # builds a URL like qt/6.11/6.12.0/ that 404s.
          $series = "${{ env.QT_VERSION }}" -replace '\.\d+$', ''
          $base = "https://download.qt.io/official_releases/qt/$series/${{ env.QT_VERSION }}/submodules"
          Invoke-WebRequest "$base/qtbase-everywhere-src-${{ env.QT_VERSION }}.zip" -OutFile qtbase.zip -TimeoutSec 600
          Expand-Archive qtbase.zip -DestinationPath C:\Qt-src

          $file = "C:\Qt-src\qtbase-everywhere-src-${{ env.QT_VERSION }}\src\gui\text\windows\qwindowsfontdatabase_ft.cpp"
          $content = [System.IO.File]::ReadAllText($file)
          $patched = $content.Replace('#include <QtCore/QSettings>', '// #include <QtCore/QSettings> -- removed: QSettings disabled')
          if ($patched -eq $content) { Write-Error "Patch target not found in $file"; exit 1 }
          [System.IO.File]::WriteAllText($file, $patched, [System.Text.UTF8Encoding]::new($false))

      - name: Configure, build & install Qt
        if: steps.qt-cache.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          # Flags come from ci/qt-windows.conf, which the cache key hashes.
          $flags = Get-Content ci\qt-windows.conf |
                   ForEach-Object { $_.Trim() } |
                   Where-Object { $_ -and -not $_.StartsWith('#') }
          $configure = "C:\Qt-src\qtbase-everywhere-src-${{ env.QT_VERSION }}\configure.bat"
          & $configure -prefix ${{ matrix.prefix }} @flags -- -B C:\Qt-build
          if ($LASTEXITCODE -ne 0) { throw "Qt configure failed" }
          cmake --build C:\Qt-build --parallel
          if ($LASTEXITCODE -ne 0) { throw "Qt build failed" }
          cmake --install C:\Qt-build
          if ($LASTEXITCODE -ne 0) { throw "Qt install failed" }

      - name: Generate Qt forwarding headers
        if: steps.qt-cache.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          foreach ($module in @('QtWidgets', 'QtGui', 'QtCore', 'QtNetwork')) {
            $dir = "C:\Qt-static\include\$module"
            if (-not (Test-Path $dir)) { continue }
            foreach ($h in Get-ChildItem $dir -Filter "q*.h") {
              $content = Get-Content $h.FullName -Raw
              $regexMatches = [regex]::Matches($content,
                '(?m)^ *(?:template\s*<[^>]*>\s*)?(?:class|struct)\s+(?:Q_\w+_EXPORT\s+)(Q[A-Za-z0-9_]+)')
              foreach ($m in $regexMatches) {
                $className = $m.Groups[1].Value
                if ($className -match 'Private$') { continue }
                $fwd = Join-Path $dir $className
                if (-not (Test-Path $fwd)) {
                  Set-Content -Path $fwd -Value "#include `"$($h.Name)`"" -Encoding UTF8 -NoNewline
                }
              }
            }
          }

      - name: Build app
        shell: cmd
        run: |
          cmake -S . -B build -G Ninja ^
            -DCMAKE_BUILD_TYPE=Release ^
            -DCMAKE_PREFIX_PATH=${{ matrix.prefix }} ^
            -DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded && ^
          cmake --build build --parallel

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.artifact }}
          path: build\OpenMTR.exe

  # macOS is built differently from Windows: dynamically-linked official Qt
  # release binaries (fetched with aqtinstall), bundled into the .app via
  # macdeployqt, rather than a fully static Qt build. Trying to build Qt
  # itself from source for macOS in CI (the way the Windows jobs do) would
  # be far slower for comparatively little benefit, since macOS apps
  # conventionally ship their dependencies bundled in the .app anyway.
  #
  # Official binaries, NOT Homebrew's qt, for two hard reasons:
  #  * Homebrew compiles for the runner's own OS release, so the bundle
  #    silently required the newest macOS (its libicuuc was minos 26.0)
  #    while Info.plist promised 12.0 — the app launched on the build
  #    machine and was killed by dyld everywhere else.
  #  * Homebrew's Qt links ICU (libicudata alone is 32 MB), glib and dbus;
  #    official Qt macOS binaries carry none of those, which is most of the
  #    difference between a ~30 MB and a single-digit-MB DMG.
  build-macos:
    runs-on: macos-latest
    name: Build (macOS ARM64)
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1

      - name: Install Qt (official binaries)
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          cache: true

      - name: Ensure Ninja
        run: command -v ninja >/dev/null || brew install ninja

      - name: Configure & build
        run: |
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH="$QT_ROOT_DIR"
          cmake --build build --parallel

      - name: Bundle Qt frameworks into the app
        run: macdeployqt build/OpenMTR.app -always-overwrite

      - name: Thin universal Qt libraries to arm64
        # Official Qt dylibs are universal (x86_64 + arm64); the app itself
        # is arm64-only (see CMAKE_OSX_ARCHITECTURES), so the x86_64 halves
        # are dead weight — roughly half of every bundled library.
        run: |
          find build/OpenMTR.app -type f | while IFS= read -r f; do
            if lipo -archs "$f" 2>/dev/null | grep -q 'x86_64'; then
              lipo "$f" -thin arm64 -output "$f"
            fi
          done

      - name: Drop plugins the app never loads
        # macdeployqt bundles every plugin its Qt modules could possibly
        # want, not the ones this app uses. Pruned before signing so the
        # signature covers what actually ships.
        #   imageformats — the only image the app decodes is the qrc'd
        #     app_icon.png, and PNG support is built into QtGui itself; the
        #     gif/ico/jpeg plugins are pure weight.
        #   iconengines  — libqsvgicon is the sole entry and nothing here
        #     loads an SVG, which also orphans the QtSvg framework.
        # Kept deliberately: platforms (libqcocoa, mandatory), styles
        # (libqmacstyle, the native look) and tls/networkinformation — the
        # update check talks HTTPS to api.github.com and silently fails
        # without a TLS backend.
        run: |
          rm -rf build/OpenMTR.app/Contents/PlugIns/imageformats
          rm -rf build/OpenMTR.app/Contents/PlugIns/iconengines
          rm -rf build/OpenMTR.app/Contents/Frameworks/QtSvg.framework
          echo "Remaining plugins:"
          find build/OpenMTR.app/Contents/PlugIns -name '*.dylib' | sort

      - name: Ad-hoc sign (inside-out)
        run: |
          # Thinning above invalidated the existing signatures. Re-sign
          # nested code first, then the bundle: codesign --deep is
          # deprecated and signs in an order Gatekeeper may reject.
          find build/OpenMTR.app/Contents/Frameworks \
               build/OpenMTR.app/Contents/PlugIns \
               -type f -print0 | while IFS= read -r -d '' f; do
            file -b "$f" | grep -q 'Mach-O' && codesign --force --sign - "$f"
          done
          codesign --force --sign - build/OpenMTR.app

      - name: Verify every bundled binary runs on the declared minimum OS
        # The exact regression this job used to ship: a bundle whose
        # Info.plist promises one macOS version but whose libraries need a
        # newer one, so it launches on the runner and dies with dyld errors
        # on users' machines. Fail the build instead. The bar is read from
        # the built Info.plist rather than hardcoded, so it tracks
        # CMAKE_OSX_DEPLOYMENT_TARGET on its own.
        run: |
          declared=$(plutil -extract LSMinimumSystemVersion raw \
                       build/OpenMTR.app/Contents/Info.plist)
          echo "Bundle declares macOS $declared"
          fail=0
          while IFS= read -r f; do
            file -b "$f" | grep -q 'Mach-O' || continue
            minos=$(otool -l "$f" | awk '/LC_BUILD_VERSION/{b=1} b && /minos/{print $2; exit}')
            [ -n "$minos" ] || continue
            # Full version compare via sort -V, not just the major component:
            # a library needing 13.4 in a bundle declaring 13.0 still crashes
            # on 13.0-13.3. sort -V also handles a three-part minos (13.4.1),
            # which splitting into major/minor by hand would truncate.
            if [ "$(printf '%s\n%s\n' "$declared" "$minos" | sort -V | tail -1)" != "$declared" ]; then
              echo "::error::$f requires macOS $minos but the bundle declares $declared"
              fail=1
            fi
          done < <(find build/OpenMTR.app -type f)
          exit $fail

      - name: Package into a DMG
        run: |
          # create-dmg rather than bare hdiutil: hdiutil can only produce the
          # app and an /Applications alias dropped into a default Finder
          # window — no window size, no icon placement, no background. It
          # drives the same Finder/AppleScript setup by hand that every
          # polished .dmg uses.
          brew install create-dmg

          # The background is optional so the workflow works before the
          # artwork exists (see packaging/dmg-background-template.svg for the
          # layout it has to match). Retina needs both sizes combined into
          # one HiDPI TIFF; a lone 1x PNG would be upscaled and soft.
          bg=()
          if [ -f packaging/dmg-background@2x.png ] && [ -f packaging/dmg-background.png ]; then
            tiffutil -cathidpicheck packaging/dmg-background.png \
                                    packaging/dmg-background@2x.png -out dmg-bg.tiff
            bg=(--background dmg-bg.tiff)
          elif [ -f packaging/dmg-background.png ]; then
            bg=(--background packaging/dmg-background.png)
          fi

          # Icon coordinates are centres in window points and must stay in
          # step with the template.
          create-dmg \
            --volname "OpenMTR" \
            --window-pos 200 120 \
            --window-size 600 400 \
            --icon-size 128 \
            --icon "OpenMTR.app" 170 190 \
            --app-drop-link 430 190 \
            --hide-extension "OpenMTR.app" \
            --no-internet-enable \
            "${bg[@]}" \
            build/OpenMTR.dmg \
            build/OpenMTR.app

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: OpenMTR-macOS
          path: build/OpenMTR.dmg

  # Linux links Qt dynamically and bundles the runtime dependencies at
  # packaging time via linuxdeploy + its Qt plugin, which play the same role
  # appimage-side that macdeployqt plays for the .app bundle above. Two
  # architectures, like the Windows matrix — but unlike Windows
  # (cross-compiled via the ARM64 MSVC toolset), each runs natively on
  # GitHub's own AMD64/ARM64 Linux runners, so there's no cross-compilation
  # to set up. Runs on ubuntu-26.04, which sets the AppImage's minimum glibc
  # (resolute's, vs. jammy's 2.35) — less portable to older/LTS distros, an
  # accepted trade-off for targeting current desktop systems.
  #
  # Qt is built from source rather than taken from apt, for one reason:
  # -no-icu. Both the distro package and Qt's own official Linux binaries
  # link libQt6Core against libicui18n/libicuuc/libicudata, so linuxdeploy
  # has to bundle ICU either way — 38 MB unpacked, ~11 MB of the AppImage,
  # by far its largest single component and bigger than everything this app
  # actually uses. Qt falls back to its own built-in codecs without it; the
  # only real loss is locale-aware collation (QCollator), which nothing here
  # uses. Same from-source approach as the Windows jobs, and cached the same
  # way, so only the first run per architecture pays for it.
  #
  # Deliberately NOT carrying over the Windows job's long -no-feature-* list:
  # that needs someone who can verify the result on a real Linux desktop.
  build-linux:
    strategy:
      matrix:
        include:
          - arch: amd64
            label: Linux AMD64
            runner: ubuntu-26.04
            linuxdeploy_arch: x86_64
            artifact: OpenMTR-Linux-AMD64
          - arch: arm64
            label: Linux ARM64
            runner: ubuntu-26.04-arm
            linuxdeploy_arch: aarch64
            artifact: OpenMTR-Linux-ARM64

    runs-on: ${{ matrix.runner }}
    name: Build (${{ matrix.label }})
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1

      - name: Restore Qt cache
        id: qt-cache
        uses: actions/cache@v6
        with:
          path: ~/qt-noicu
          # Hashing this job's own flag file keeps it independent of the
          # Windows caches: editing Linux's Qt configuration rebuilds only
          # Linux's Qt.
          key: qt-noicu-${{ env.QT_VERSION }}-${{ matrix.arch }}-${{ hashFiles('ci/qt-linux.conf') }}

      - name: Install build dependencies
        # Qt's xcb platform plugin needs the whole xcb dev set present at
        # configure time or it is silently skipped, leaving a Qt that cannot
        # open a window — hence the long list, and the explicit check after
        # the install step below.
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential ninja-build pkg-config libfuse2 \
            libgl1-mesa-dev libegl1-mesa-dev \
            libfontconfig1-dev libfreetype-dev \
            libx11-dev libx11-xcb-dev libxext-dev libxfixes-dev libxi-dev \
            libxcb1-dev libxcb-cursor-dev libxcb-glx0-dev libxcb-icccm4-dev \
            libxcb-image0-dev libxcb-keysyms1-dev libxcb-randr0-dev \
            libxcb-render-util0-dev libxcb-render0-dev libxcb-shape0-dev \
            libxcb-shm0-dev libxcb-sync-dev libxcb-util-dev \
            libxcb-xfixes0-dev libxcb-xinerama0-dev libxcb-xkb-dev \
            libxkbcommon-dev libxkbcommon-x11-dev \
            libdbus-1-dev libglib2.0-dev zlib1g-dev libssl-dev

      - name: Fetch Qt source
        if: steps.qt-cache.outputs.cache-hit != 'true'
        run: |
          # Series (6.11) derived from QT_VERSION (6.11.1) — see the Windows
          # job for why it isn't written out a second time.
          series="${QT_VERSION%.*}"
          base="https://download.qt.io/official_releases/qt/$series/${QT_VERSION}/submodules"
          wget -q "$base/qtbase-everywhere-src-${{ env.QT_VERSION }}.tar.xz"
          tar xf "qtbase-everywhere-src-${{ env.QT_VERSION }}.tar.xz"

      - name: Configure, build & install Qt
        if: steps.qt-cache.outputs.cache-hit != 'true'
        run: |
          # Flags come from ci/qt-linux.conf, which the cache key hashes.
          # Read before the cd, since that file is in the workspace.
          # awk 'NF{...}' drops blank lines first, then trims — the NF test
          # has to come before touching $1, since assigning to it on an empty
          # record creates a field and makes NF true. Not sed with \s either:
          # that is a GNU extension, and a BSD sed matches a literal "s",
          # quietly turning -nomake examples into -nomake example.
          mapfile -t QT_FLAGS < <(sed 's/#.*//' "$GITHUB_WORKSPACE/ci/qt-linux.conf" \
                                  | tr -d '\r' | awk 'NF{$1=$1; print}')
          echo "Qt flags: ${QT_FLAGS[*]}"
          cd "qtbase-everywhere-src-${{ env.QT_VERSION }}"
          ./configure -prefix "$HOME/qt-noicu" "${QT_FLAGS[@]}" -- -B "$HOME/qt-build"
          cmake --build "$HOME/qt-build" --parallel
          # --strip: a from-source Qt keeps its .symtab/.strtab, which the
          # distro packages ship stripped (their symbols live in separate
          # -dbgsym packages). 7.5 MB across the libraries linuxdeploy
          # bundles, for symbols no end user has any use for.
          cmake --install "$HOME/qt-build" --strip

      - name: Verify the Qt build is usable
        # A Qt without the xcb platform plugin builds and installs fine and
        # then fails at runtime with "could not load the Qt platform plugin",
        # which no CI step would otherwise catch. Also re-checks the one
        # thing this from-source build exists to guarantee.
        run: |
          plugin="$HOME/qt-noicu/plugins/platforms/libqxcb.so"
          if [ ! -f "$plugin" ]; then
            echo "::error::Qt built without the xcb platform plugin — the app could not open a window."
            ls -R "$HOME/qt-noicu/plugins" || true
            exit 1
          fi
          if readelf -d "$HOME/qt-noicu/lib/libQt6Core.so" | grep -qi icu; then
            echo "::error::libQt6Core still links ICU despite -no-icu."
            exit 1
          fi
          echo "xcb plugin present; libQt6Core is ICU-free."

      - name: Configure & build
        run: |
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH="$HOME/qt-noicu" \
            -DCMAKE_INSTALL_PREFIX=/usr
          cmake --build build --parallel

      - name: Install into AppDir
        run: cmake --install build --prefix AppDir/usr

      - name: Find what pulls in ICU
        run: |
          for f in $(find AppDir -name '*.so*'); do
            readelf -d "$f" 2>/dev/null | grep -q 'NEEDED.*libicu' && echo "$f linkuje ICU"
          done

      - name: Fetch linuxdeploy & its Qt plugin
        run: |
          base="https://github.com/linuxdeploy"
          wget -q "$base/linuxdeploy/releases/download/continuous/linuxdeploy-${{ matrix.linuxdeploy_arch }}.AppImage"
          wget -q "$base/linuxdeploy-plugin-qt/releases/download/continuous/linuxdeploy-plugin-qt-${{ matrix.linuxdeploy_arch }}.AppImage"
          chmod +x linuxdeploy-*.AppImage

      - name: Drop transitive ICU dependency
        run: find AppDir -name 'libicu*' -delete

      - name: Build AppImage
        env:
          # Squashing the AppImage's own FUSE mount inside a container
          # without --privileged is unreliable; extract-and-run avoids it.
          APPIMAGE_EXTRACT_AND_RUN: 1
        run: |
          # linuxdeploy needs qmake to locate the Qt install — point it at
          # the from-source build rather than anything apt may have left on
          # the runner, or the AppImage would be filled with the wrong Qt
          # (and with the ICU this job exists to avoid). Exported here rather
          # than in env:, which does not expand $HOME.
          export QMAKE="$HOME/qt-noicu/bin/qmake"
          # linuxdeploy resolves each ELF dependency through the loader's
          # normal search path. The from-source Qt lives outside it (an apt
          # Qt would have been under /usr/lib), so without this it fails with
          # "Could not find dependency: libQt6Widgets.so.6".
          export LD_LIBRARY_PATH="$HOME/qt-noicu/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
          ./linuxdeploy-${{ matrix.linuxdeploy_arch }}.AppImage \
            --appdir AppDir \
            --plugin qt \
            --output appimage \
            --desktop-file openmtr.desktop \
            --icon-file app_icon.png

      - name: Rename artifact
        run: mv OpenMTR*.AppImage OpenMTR-${{ matrix.arch }}.AppImage

      - name: Verify the AppImage ships no ICU
        # linuxdeploy pulls in whatever the binaries link against, so a Qt
        # that quietly regained ICU (a cache built from an older config, an
        # apt Qt winning the lookup) would silently put ~11 MB back. Cheap
        # to assert, and the failure names the cause.
        run: |
          ./OpenMTR-${{ matrix.arch }}.AppImage --appimage-extract >/dev/null
          if find squashfs-root -name 'libicu*' | grep -q .; then
            echo "::error::AppImage contains ICU — the Qt it bundled was not the -no-icu build."
            find squashfs-root -name 'libicu*'
            exit 1
          fi
          du -sh OpenMTR-${{ matrix.arch }}.AppImage
          rm -rf squashfs-root

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.artifact }}
          path: OpenMTR-${{ matrix.arch }}.AppImage
